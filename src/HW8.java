public class HW8 {
    public static void main(String... args)
    {
    	//	Create a PublisherImpl object
    	//	Create an EventGenerator object
    	//	Create a SubscriberOdds class, which implements the Subscriber interface.
    	//	The threshold is 21
    	//	Create a SubscriberEvens class, which implements the Subscriber Interface
    	//	The threshold is 14
    	//	Create a SubscriberThrees class, which implements the Subscriber Interface.
    	//	The threshold is 10.
    	//	pub.registerSubscriber() for each of the Subscriber objects created
    	//	eventGen.runSimulation()
    }
}
